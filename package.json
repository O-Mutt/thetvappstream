{
  "name": "thetvappstream",
  "version": "2.0.0",
  "description": "HLS scraper for thetvapp.to — generates M3U playlists and per-channel signed stream URLs.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
